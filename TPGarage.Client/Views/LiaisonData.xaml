<UserControl x:Class="TPGarage.Client.Views.LiaisonData"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TPGarage.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <TextBox x:Name="_TextBoxData" Grid.Row="0"/>
    <TextBox Grid.Row="1" Text="{Binding Path=Text , ElementName=_TextBoxData ,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"/>
  </Grid>
</UserControl>
